<Window x:Class="StyleSwitch.Window1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="StyleSwitch" Height="300" Width="300" Background="{DynamicResource AppBackground}">

  <Grid>

    <Grid.RowDefinitions>
      <RowDefinition />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel>
      <TextBlock Style="{DynamicResource TitleText}">This is some text</TextBlock>
      <Button Style="{DynamicResource MainButton}">This is the main button</Button>
      <CheckBox>This is a check box</CheckBox>
      <Expander Style="{DynamicResource AdvancedSettings}" Header="Advanced Settings">
        <ToggleButton>Turn on Advanced settings!</ToggleButton>
      </Expander>
    </StackPanel>

    <ComboBox Margin="10" Grid.Row="1" SelectedIndex="0" SelectionChanged="OnThemeChanged">
      <ComboBoxItem Content="Blue Theme" Tag="bluetheme.xaml" />
      <ComboBoxItem Content="Red Theme" Tag="redtheme.xaml" />
    </ComboBox>
    
  </Grid>
  
</Window>
