<UserControl x:Class="UserControls.UpDownControl" x:Name="udControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>            
        
        <TextBox x:Name="tb" Text="{Binding ElementName=udControl, Path=Value}" />
        
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <RepeatButton x:Name="upBtn" Width="16" Height="16">
                <Path Stroke="Black" Fill="Black" StrokeLineJoin="Round" Data="M5,1 L1,9 L9,9Z" />
            </RepeatButton>
            <RepeatButton x:Name="dnBtn" Width="16" Height="16" Grid.Row="1">
                <Path Stroke="Black" Fill="Black" StrokeLineJoin="Round" Data="M5,9 L1,1 L9,1Z" />
            </RepeatButton>
        </Grid>
        
    </Grid>
</UserControl>
